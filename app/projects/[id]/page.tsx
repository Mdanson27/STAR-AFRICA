import type { Metadata } from 'next';import { notFound } from 'next/navigation';import { AppShell } from '@/components/app-shell';import { ProjectWorkspace } from '@/components/projects/project-workspace';import { loadProject } from '@/lib/projects/data';import { requireAuthorizedSession } from '@/lib/security/session';
export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{id:string}>}):Promise<Metadata>{const {id}=await params;const data=await loadProject(id);return{title:data?.project.name??'Project workspace',description:data?.project.description??'Operational project command centre'}}
export default async function ProjectPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const session=await requireAuthorizedSession('projects.view',`/projects/${id}`);const data=await loadProject(id);if(!data)notFound();return <AppShell active="Projects" user={session}><ProjectWorkspace data={data} permissions={[...session.permissions]} currentUser={session.name}/></AppShell>}
